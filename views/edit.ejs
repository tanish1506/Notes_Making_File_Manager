<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white p-10 flex justify-center" >
        <div class="w-[80%] h-fit flex flex-col items-center bg-zinc-800 p-5 rounded-xl gap-5"  >
            <a class="absolute  left-[1%] bg-zinc-500 px-2 py-1 rounded-lg font-semibold" href="/">Go Back to home</a>
            
            <form class="w-[100%] flex items-center flex-col gap-3 m-auto pt-3" action="/edit" method="post">
                <div class=" w-full flex ">
                    <h2 class="w-[50%] text-center font-bold">Previous</h2>
                    <h2 class="w-[50%] text-center font-bold">New</h2>
                </div>
                <div class="w-full flex gap-2">
                    <input class=" w-[50%] block  px-2 py-1 bg-zinc-700 text-zinc-400 rounded-lg outline-none "   type="text" placeholder="Previous Name" name="previous" value="<%= filename%>">
                    <input class=" w-[50%] block  px-2 py-1 bg-zinc-700 rounded-lg outline-none"  type="text" placeholder="New Name" name="new">
                </div>
                <div class="w-full flex gap-2">  
                    <p class=" w-[100%] text-wrap h-full block px-2 py-1 bg-zinc-700 rounded-lg text-sm" placeholder=" your task details here" rows="5" cols="" name="olddetails" ><%= filedata%></p>
                    <textarea class=" w-[100%] block px-2 py-1 bg-zinc-700 rounded-lg outline-none resize-none" placeholder="Update your task details here" rows="5" cols="" name="newdetails"></textarea>
                </div>
                    <input class="bg-black rounded-lg px-3 py-1 font-semibold" type="submit" value="Update">
            </form>
            
        </div>
    </div>
</body>
</html>