<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 color white">
        <h1 class="text-center text-2xl font-bold py-3 text-yellow-200">Notes Making File Managerr</h1>
        <div class="form text-white">
                <form class=" flex items-center flex-col gap-3 m-auto pt-3" action="/create" method="post">
                    <input class=" w-[50%] block  px-2 py-1 bg-zinc-800 rounded-lg outline-none" class="w-[60%] bg-white" type="text" placeholder="Title goes here : " name="title">
                    <textarea class=" w-[50%] block px-2 py-1 bg-zinc-800 rounded-lg outline-none resize-none" placeholder="Write your task details here" rows="" cols="" name="details"></textarea>
                    <input class="bg-black rounded-lg px-3 py-1 font-semibold" type="submit" value="Create task">
                </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap ">
            <% if(files.length > 0){ %>
                <% files.forEach(function(val){ %>

                    <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-lg ">
                        <h1 class="text-white text-2xl font-semibold tracking-tighter"><%= val %></h1>
                        <div class="flex justify-between items-center">
                            <a class=" text-blue-600 underline inline-block " href="/files/<%= val %>" >Read More</a>
                            <a class="text-zinc-600 " href="/edit/<%= val%>">Edit</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                    <h3 class="text-zinc-600 m-auto">No task yet....</h3>
            
            <% } %>
       </div>
        </div>

    </div>
</body>
</html>